@page
@model SavedModel
@{
    ViewData["Title"] = "Zapisane";
}

<p>Dane w zapisane w sesji:</p>

@{
    if(Model.YearResponses != null)
    {
        <ol>
        @for(int i = 0; i < Model.YearResponses.Count; i++)
        {
            <li>@Model.YearResponses[i] </li>

        }
        </ol>
    }
}
